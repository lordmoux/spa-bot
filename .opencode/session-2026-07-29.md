# spa-bot — Session Summary (2026-07-29)

## Changes Made

### Demo chat endpoint + web UI
- `POST /demo/chat` — endpoint simple que acepta `{"message":"..."}`
- `GET /demo` — sirve `public/demo.html`
- `public/demo.html` — chat tipo WhatsApp en burbujas

### Migración IA (DeepSeek → Groq, gratis)
- `.env` usa `AI_API_KEY`, `AI_BASE_URL`, `AI_MODEL`
- `src/services/openaiService.js` actualizado para Groq (OpenAI SDK compatible)
- Modelo: `llama-3.1-8b-instant`

### Versión Termux (android)
- `~/spa-bot-termux/` — copia con `sql.js` en vez de `better-sqlite3`
- `src/config/database.js` — wrapper sql.js con persistencia a disco
- `src/index.js` — initDatabase async

### Deploy Railway
- `.gitignore` usa `/data/` (no ignora `src/data/`)
- Eliminado `allowScripts` de `package.json`
- Repo: `lordmoux/spa-bot`
- URL: `https://spa-bot-production.up.railway.app/demo`

## Estado
Demo funcional. Pendiente catálogo real de la amiga.
